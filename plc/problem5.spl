start{
  fib = [1, 1];
  seen = [];
}

0-1{
  return $0;
  seen = seen++[$0];
}

2+{
  seen = seen++[$0];
  x = zip seen fib;
  x = {(fst buffer) *(snd buffer) | buffer <- x};
  return (sum x);
  a = last fib;
  b = head (init fib);
  fib = (a+b):fib;
}
