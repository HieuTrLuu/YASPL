int acc = 0;
stream fib;
for a in $0 {
  int fibNext = if (acc < 2) then 1 else fib[acc] + fib[acc-1];
  fib += fibNext;
  int out = 0;
  for (int i=0;i<=acc;i++) {
    out += $0[i] * fib[acc-i];
  }
  acc++;
  print out;
}

//no token for stream.
=> declare stream fib as list fib