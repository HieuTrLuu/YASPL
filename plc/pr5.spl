start{
  fib = [1, 1];
  seen = [];

}
0-1{
  return $0;
  seen = [$0];
}

0+{
  seen = seen++[$0];
  x = zip seen (reverse fib);
  x = {(fst z) * (snd z) | z <- x};
  return (sum x);
  a = last fib;
  b = head (tail (reverse fib));
  fib = fib++[(a+b)];
}
